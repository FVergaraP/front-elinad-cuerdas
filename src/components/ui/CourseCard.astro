---
import type { Curso } from '@/data/courses';
import { contenido } from '@/data/content';
import WhatsappButton from '@/components/ui/WhatsappButton.astro';

interface Props {
  course: Curso;
}

const { course } = Astro.props;

const priceFormatted = new Intl.NumberFormat('es-CL', {
  style: 'currency',
  currency: course.moneda,
  maximumFractionDigits: 0,
}).format(course.precio);

// Las clases van escritas literalmente para que Tailwind las incluya en el CSS.
// El `?? ''` es la red por si el valor del JSON no coincide con ninguna clave.
const modalityColors: Record<string, string> = {
  Presencial: 'text-green-400 bg-green-400/10',
  Online:     'text-blue-400  bg-blue-400/10',
  Híbrido:    'text-yellow-400 bg-yellow-400/10',
};

const levelColors: Record<string, string> = {
  'Principiante':     'text-emerald-400  bg-emerald-400/10',
  'Intermedio':       'text-amber-400    bg-amber-400/10',
  'Avanzado':         'text-red-400      bg-red-400/10',
  'Todos los niveles':'text-theme-accent bg-theme-accent/10',
};
---

<article class="card flex flex-col group h-full" aria-label={`Curso: ${course.titulo}`}>
  <!-- Badge de nivel + modalidad -->
  <div class="flex items-center gap-2 p-4 pb-0 flex-wrap">
    <span class={`text-xs font-semibold px-2.5 py-1 rounded-full ${levelColors[course.nivel] ?? ''}`}>
      {course.nivel}
    </span>
    <span class={`text-xs font-semibold px-2.5 py-1 rounded-full ${modalityColors[course.modalidad] ?? ''}`}>
      {course.modalidad}
    </span>
    {course.destacado && (
      <span class="ml-auto text-xs font-bold px-2.5 py-1 rounded-full bg-theme-accent/20 text-theme-accent">
        Destacado ✦
      </span>
    )}
  </div>

  <!-- Contenido -->
  <div class="flex flex-col flex-1 p-5 gap-3">
    <h3 class="text-xl font-display font-bold text-theme-text group-hover:text-theme-accent transition-colors">
      {course.titulo}
    </h3>

    <p class="text-theme-muted text-sm leading-relaxed flex-1 whitespace-pre-line">
      {course.descripcion}
    </p>

    <!-- Detalles -->
    <ul class="grid grid-cols-2 gap-x-4 gap-y-1.5 text-xs text-theme-muted mt-1" aria-label="Detalles del curso">
      <li class="flex items-center gap-1.5">
        <svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" viewBox="0 0 24 24"
             fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
             stroke-linejoin="round" aria-hidden="true">
          <circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>
        </svg>
        {course.duracion}
      </li>
      <li class="flex items-center gap-1.5">
        <svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" viewBox="0 0 24 24"
             fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
             stroke-linejoin="round" aria-hidden="true">
          <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>
        </svg>
        {course.frecuencia}
      </li>
    </ul>

    <!-- Tags -->
    <div class="flex flex-wrap gap-1.5" aria-label="Etiquetas">
      {course.etiquetas.map((tag) => (
        <span class="text-xs px-2 py-0.5 rounded-md bg-theme-border/50 text-theme-muted">
          #{tag}
        </span>
      ))}
    </div>
  </div>

  <!-- Footer: precio + CTAs -->
  <div class="border-t border-theme-border p-5 flex flex-col gap-3">
    <div class="flex items-baseline gap-1">
      <span class="text-2xl font-bold font-display text-theme-accent">{priceFormatted}</span>
      <span class="text-xs text-theme-muted">{course.sufijoPrecio}</span>
    </div>

    <WhatsappButton
      message={course.mensajeWhatsapp}
      numero={contenido.contacto.whatsappNumero}
      label="Más información"
      class="w-full justify-center"
      aria-label={`Consultar por WhatsApp sobre ${course.titulo}`}
    />
  </div>
</article>
